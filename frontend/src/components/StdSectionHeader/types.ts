export interface SectionHeaderProps {
  /** Small eyebrow label shown above the title. */
  label: string
  /** Main heading text. */
  title: string
  /** Accent word/phrase rendered in a highlighted span after the title. */
  accent: string
}
